---

render_variables_hcl_:
  type: jinja
  template_path: nuki/templates/variables.hcl.j2
  output_path: variables.hcl
  extra_context: "{{ deploy.deployments_[deploy.index] }}"

run_terragrunt_:
  type: shell
  command: terragrunt apply-all --terragrunt-non-interactive
